{
  "name": "vocechat",
  "version": "0.4.4",
  "description": "Portable OpenClaw VoceChat channel plugin",
  "type": "module",
  "keywords": [
    "openclaw",
    "plugin",
    "vocechat",
    "channel"
  ],
  "files": [
    "index.ts",
    "openclaw.plugin.json",
    "README.md",
    "CHANGELOG.md",
    "config/**/*",
    "src/**/*"
  ],
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "undici": "^7.16.0"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
