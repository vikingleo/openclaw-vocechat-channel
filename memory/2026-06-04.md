- 2026-06-04：修复 VoceChat direct chat 中间回复刷屏：`src/dispatch-run-events.ts` 原本会把 direct chat 的 `onPartialReply` 发成 `partial_reply` 消息，导致一次回复过程中出现多条“回复生成中/回复片段”。改为 `onPartialReply: noopRunEventCallback`，保留最终回复和 direct chat 的工具/执行事件。新增回归测试 `direct chat partial replies are suppressed`。验证：目标测试先红后绿、`npm test`、`git diff --check` 通过。已运行 `sh scripts/sync-to-root-extension.sh` 同步到 `/home/vkleo/.openclaw/extensions/vocechat` 并构建，随后重启 `openclaw-gateway.service`；服务 active，VoceChat 插件 17:00:55 重新加载并 ready。
- 2026-06-04：继续收紧 VoceChat 聊天输出：用户要求关闭所有执行事件、执行动作、命令输出，只保留结果回复。将 `src/dispatch-run-events.ts` 收缩为全通道 noop bridge，`onToolStart/onToolResult/onCommandOutput/onPatchSummary/onPlanUpdate/onApprovalEvent/onItemEvent/onPartialReply/onReasoningStream/onBlockReplyQueued` 全部静默，并保留 `suppressDefaultToolProgressMessages: true`。更新回归测试 `dispatch bridge suppresses execution events for direct chat`，覆盖 direct chat 中工具、命令、审批、item、partial、reasoning、block 事件均不投递。验证：目标测试先红后绿、`npm test` 20/20、`git diff --check` 通过。已同步安装目录并重启 gateway；服务 active，VoceChat 插件 17:27:40 ready。
