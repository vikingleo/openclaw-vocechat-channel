# 2026-05-15

## OpenClaw generic failure recovery

- User wanted to root-cause frequent gateway replies:
  `Something went wrong while processing your request. Please try again, or use /new to start a fresh session.`
- Evidence pointed to OpenClaw core runner rather than the VoceChat channel plugin:
  - `/tmp/openclaw/openclaw-2026-05-14.log` had context overflow diagnostics for `agent:main:vocechat:default:direct:1`.
  - Logs also showed incomplete embedded turns with `payloads=0`, which were surfaced to the user as generic failures.
- A hot patch was already present in
  `/home/vkleo/.npm-global/lib/node_modules/openclaw/dist/agent-runner.runtime-DQsCsHUA.js`.
- Made that patch durable with startup script:
  `/home/vkleo/.openclaw/workspace/memory/openclaw-patches/patch-agent-empty-terminal-recovery.sh`.
- Added systemd user drop-in:
  `/home/vkleo/.config/systemd/user/openclaw-gateway.service.d/52-empty-terminal-recovery-patch.conf`.
- Script behavior:
  - Adds recoverable empty-terminal detection.
  - Resets the affected session via existing `resetSessionAfterCompactionFailure` for final context overflow with no payload text.
  - Resets the affected session for empty abandoned terminal replies when there is no outbound delivery/tool/cron/silent-reply evidence.
  - Leaves provider rate limits, billing/auth failures, explicit tool deliveries, cron additions, and intentional silent replies alone.
- Verification:
  - RED check: script path missing failed before implementation.
  - GREEN check: script patched a temporary copy of the original backup runtime and `node --check` passed.
  - Real runtime check: script reported already patched and `node --check` passed.
  - `systemctl --user daemon-reload` loaded the new drop-in.
  - `openclaw gateway restart --json` returned ok/restarted.
  - `systemctl --user status openclaw-gateway.service` showed `patch-agent-empty-terminal-recovery.sh` exit status 0.
  - `openclaw health --json` returned ok, plugins loaded, VoceChat connected, eventLoop degraded false after settling.
  - `/queue/status` and `/vocechat/queue/status` both returned ok with empty queues.
- Residual risk:
  - This is a local durable startup patch, not an upstream OpenClaw source fix.
  - It does not fix provider-side concurrency/rate-limit failures.
  - Existing `patch-agent-slash-command-dispatch.sh` still exits 1 on startup because its pattern is not found; the service tolerates it due `ExecStartPre=-`.

## OpenClaw agent slash command startup patch cleanup

- User agreed to fix the remaining `patch-agent-slash-command-dispatch.sh` startup failure.
- Root cause:
  - The script only scanned old `server-plugin-bootstrap-*.js` files.
  - OpenClaw 2026.5.7 currently keeps the agent RPC handler in `server-methods-DStUV8Sh.js`.
  - The script also hard-coded an obsolete `commands.runtime-*` chunk name.
- Fix:
  - Updated `/home/vkleo/.openclaw/workspace/memory/openclaw-patches/patch-agent-slash-command-dispatch.sh`.
  - It now scans both `server-plugin-bootstrap-*.js` and `server-methods-*.js`.
  - It resolves the current `commands.runtime-*.js` chunk dynamically.
  - It handles timestamp-injected agent messages by using the injected command when recoverable, otherwise falling back to the raw request message for non-reset slash commands.
- Verification:
  - `bash -n patch-agent-slash-command-dispatch.sh` passed.
  - Temporary-copy patch test passed and `node --check` passed.
  - Real `/home/vkleo/.npm-global/lib/node_modules/openclaw/dist/server-methods-DStUV8Sh.js` patched and `node --check` passed.
  - `openclaw gateway restart --json` returned ok/restarted.
  - `systemctl --user status openclaw-gateway.service` showed `patch-agent-slash-command-dispatch.sh` exit status 0.
  - `openclaw agent --agent main --message '/status' --json --timeout 20` returned `summary: "command"` with gateway status payload.
  - `openclaw health --json` returned ok; VoceChat stayed connected.
- Residual risk:
  - Existing `patch-media-local-roots-works.sh` still logs non-fatal warnings for unrelated stale embedded-session retry patterns and unwritable `/usr/local` OpenClaw files. It exits 0 and did not block gateway startup.
  - Later health checks still showed short-window `eventLoop.degraded=true` due CPU/event-loop utilization, while service stayed `ok: true`. This is a separate runtime performance signal, not the repaired slash-command startup failure.

## OpenClaw media/root startup patch residual cleanup

- User asked whether the independent residual could be fixed.
- Targeted the leftover non-fatal warnings from
  `/home/vkleo/.openclaw/workspace/memory/openclaw-patches/patch-media-local-roots-works.sh`.
- Root causes:
  - `/usr/local/lib/node_modules/openclaw/dist` was a stale root-owned OpenClaw 2026.2.25 install; active service and CLI use `/home/vkleo/.npm-global/lib/node_modules/openclaw` version 2026.5.7.
  - `selection-*.js` still needed the embedded retry patch, but upstream changed the call-site shape by introducing `piAutoCompactionGuardArgs`.
  - `cli-compaction-*.js` matched the broad collector but is outside the gateway embedded-runner retry patch surface.
- Fix:
  - Updated `patch-media-local-roots-works.sh` to skip inactive stale dist directories when a current home install exists.
  - Restricted embedded-session retry scanning to `compact-*.js` and `selection-*.js`.
  - Added a current `selection-*.js` pattern variant and patched `/home/vkleo/.npm-global/lib/node_modules/openclaw/dist/selection-BeP8qtCb.js`.
- Verification:
  - RED command failed before the fix because script output contained `WARN:` and non-zero missed counters.
  - `bash -n patch-media-local-roots-works.sh` passed.
  - Idempotent rerun produced no `WARN:` and all `*_missed` counters were 0.
  - `node --check /home/vkleo/.npm-global/lib/node_modules/openclaw/dist/selection-BeP8qtCb.js` passed.
  - `openclaw gateway restart --json` returned ok/restarted.
  - `systemctl --user status openclaw-gateway.service` showed all three startup patches exit status 0.
  - Startup journal showed `patch-media-local-roots-works.sh` summary with `missed=0` and `embedded_retry_missed=0`.
  - `openclaw health --json` returned `ok: true`, `eventLoop.degraded: false`, and VoceChat connected.
- Residual risk:
  - This remains a local durable startup patch rather than an upstream OpenClaw source fix.
  - The stale `/usr/local` OpenClaw install is left in place, but no longer treated as an active patch target while the 2026.5.7 home install is active.

## sub2api upstream Invalid token 401

- User reported client error: `unexpected status 401 Unauthorized: Invalid token`, URL `https://ai.vkleo.cn:4080/responses`, upstream request id `202605150717537011739688268d9d6THx16Fr9`, client request id `f29b576d-7a4c-4923-87f3-72184a3e3549`.
- Evidence:
  - `ai.vkleo.cn:4080` is OpenResty `sub2api.conf` proxying to local `sub2api` on host port `8080`.
  - OpenResty access log showed repeated `POST /responses` and `/v1/responses` failures around 15:14-15:25 CST.
  - `sub2api` logs showed `openai.forward_failed`, not client API-key auth failure.
  - `ops_error_logs` matched the exact upstream request id at `2026-05-15 15:17:53+08`, `account_id=6`, `status_code=401`, `error_message=Invalid token (...)`.
  - The failing upstream account is `accounts.id=6`, name `欢喜哥-200刀`, in group `GPT5.4-5.5`; client API keys were valid enough to reach forwarding.
- Fix/mitigation:
  - Set account `id=6` `schedulable=false`.
  - Added `temp_unschedulable_reason='disabled after repeated upstream 401 Invalid token on 2026-05-15'`.
  - Added `error_message='upstream 401 Invalid token; credentials need replacement'`.
- Verification:
  - Group `GPT5.4-5.5` schedulable accounts after change: `LinAI-max` and `LinAI-pro`; `id=6` no longer schedulable.
  - No new `ops_error_logs` rows for `account_id=6`/401 after `2026-05-15 15:28:30+08` during the immediate check.
  - New successful `/v1/responses` traffic after the change used `account_id=14`, not `6`.
- Residual risk:
  - Account `id=6` still needs credential replacement before re-enabling.
  - Other upstream accounts showed separate 502/503/probe timeout signals; this fix only addresses the exact 401 Invalid token path.
